# A股下周罗盘

一个面向“下周一至周五大盘方向”的研究系统。它输出概率、收益区间、模型分歧、样本外回测与失效条件，不承诺盈利，也不连接券商自动下单。

## 当前方法

- 数据：上证指数、上证50、沪深300、中证500、中证1000、创业板指、恒生指数、标普500、纳斯达克和道琼斯的日线量价；腾讯证券主源，东方财富备用，本地缓存兜底。
- 特征：多周期动量、均线距离、波动率、RSI、振幅、量能、回撤、大小盘相对强弱、跨指数离散度。
- 模型：正则化逻辑回归、直方图梯度提升、随机森林；模型按滚动样本外准确率动态加权。
- 区间：Ridge、稳健梯度回归和历史相似状态共同估计，限制极端外推。
- 验证：5 折扩展窗口时序验证，训练/测试间留出 5 个交易日间隔；策略回测含单边 3bp 成本。
- 逐日验证：分别报告未来第 1–5 个交易日的全样本、最近 120 次、高置信区间命中率和覆盖率；只有同时超过近期基线与高置信门槛才显示中置信方向。
- 行业轮动：使用申万一级行业指数覆盖电子、计算机、通信、电力设备、公用事业、军工、汽车、医药、银行、非银、有色和食品饮料，并额外合成“科技综合”。行业绝对涨跌和相对沪深300强弱分别建模。
- 行业验证：行业面板按交易日期成组切分，避免同一天的数据跨训练集和测试集；页面同时报告最近 120 次第1日方向准确率、多数类基线和相对强弱概率。
- 事件冲击层：使用可审计的事件日历，按A股实际反应日聚合重大IPO、央行会议、宏观数据和海外科技财报；事件只改变风险标记和确认条件，不擅自修改未经回测的模型概率。
- 突发监控：额外列出政策监管、半导体限制、中报暴雷和地缘商品跳价的官方核验来源，未确认消息不进入方向信号。
- 观察池：从新浪当日成交额靠前的 A 股中预筛，再用腾讯 260 日复权行情按趋势、相对沪深300强弱、RSI、波动和流动性排序。观察池尚未完成无幸存者偏差的横截面回测，不是买入推荐。

## 启动

```powershell
py -m pip install -r requirements.txt
py automation.py --build
py -m uvicorn server:app --reload
```

然后打开 <http://127.0.0.1:8000>。页面右上角“重新计算”会调用同一套自动更新流水线。首次加入行业模型后，完整训练通常需要约 2–3 分钟。

## 版本13：超短线细分领域训练

版本13把原先“只看大盘/一级行业”的结构拆成两层：

1. **日线层**继续负责隔日与未来五日的市场环境、风险开关和一级行业相对强弱；它不会被误写成盘中信号。
2. **盘中层**在 `09:35、10:00、10:30、11:00、13:30、14:00、14:30、14:50` 固定采集上证、沪深300、创业板、全市场宽度及概念板块快照，并在收盘后分别用“该时点至收盘的指数变化”和“该概念板块从快照到收盘的变化”结算标签。
3. 细分领域先覆盖半导体设备、存储芯片、CPO/光模块、AI算力、人形机器人、数据中心电源、智能电网、储能、火电、电力改革、创新药、医疗服务、军工电子、商业航天、铜、证券、消费电子、汽车零部件。未积累足够样本时，它们显示为**热度观察**而非预测结论。
4. 大盘盘中模型和每个细分领域模型分别计数；每个模型达到至少 **60 个已结算交易日且 240 个已结算快照**后，才会启动方向模型，并按日期前推做样本外验证；达不到门槛不会输出“盘中看涨/看跌”。
5. 在独立细分样本达标前，页面仍会输出**临时迁移倾向**：以已验证的一级行业第1日模型为先验，叠加封顶后的概念板块盘中变化，显示“临时偏强 / 临时偏弱 / 临时震荡”；该层不改写原始概率，且会标记低置信和“迁移”阶段。

在本机安装固定时点采集任务：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/install_intraday_task.ps1
```

手工采集一条快照或查看训练进度：

```powershell
py intraday_update.py
py intraday_update.py --status
```

仓库还包含 `.github/workflows/collect-intraday.yml` 作为云端后备采集；GitHub 定时任务可能延迟，本机任务更适合严格时点的训练数据。

## 版本7自动更新

自动更新入口为：

```powershell
py automation.py --build
```

流水线依次执行：

1. 抓取最新宽基、外围市场、行业、市场宽度和观察池数据。
2. 重新训练大盘与行业模型并运行滚动样本外验证。
3. 合并仅标记为“已确认”且带官方来源的短期事件。
4. 检查数据日期、5日结构、行业覆盖、验证样本、异常数值和日期倒退。
5. 门禁通过后原子替换 `public/data/forecast.json`；失败时保留上一版。
6. 写入 `data/automation/last_run.json` 和轮转日志，便于排错。

仅测试流程而不覆盖正式预测：

```powershell
py automation.py --dry-run
```

安装 Windows 定时任务：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/install_windows_task.ps1
```

默认周一至周五 22:30、周日 20:30 执行。工作日提前手动触发会先经过北京时间窗口门禁，数据尚未稳定时延后更新，避免重复失败通知。任务只在当前用户登录时运行；错过时间后会在系统可用时补跑。
任务在 Windows 任务计划程序中的名称是 `AShareCompass_AutoUpdate`。

> 当前公开站点是静态部署。定时任务会自动生成、校验并构建发布包，但不会持久保存短期部署令牌，因此线上更新仍需把通过门禁的版本发布到 Sites。这样可以避免把部署密钥写入磁盘。

## 与 ChatGPT 账号解耦的云端发布

项目包含 `.github/workflows/update-forecast-pages.yml`。把仓库放到 GitHub 并在
`Settings → Pages → Build and deployment` 中选择 `GitHub Actions` 后，云端会：

1. 工作日北京时间 22:30、周日 20:30 自动抓取、重训和运行质量门禁。
2. 门禁通过后发布 `public/` 到 GitHub Pages，并在线核验版本、5日结构和行业覆盖。
3. 把通过门禁的预测 JSON 提交回仓库，供下一次日期倒退检查使用。
4. 失败时停止发布，保留上一版 Pages，并保存 14 天诊断日志。

GitHub Pages 与 ChatGPT 账号无关；以后切换 ChatGPT 账号不需要更改站点项目编号。
首次推送到 GitHub 后，可在 `Actions` 页面手动运行一次
`Update forecast and deploy Pages`。公开仓库使用 GitHub Free 即可。

## 版本8：真实交易日、自动事件和实盘追踪

版本8新增三层防护：

1. 使用上交所官方年度休市安排生成未来5个真实A股交易日，并把核验截止日
   写入预测元数据；春节、国庆等休市日不会进入预测。若尚未录入下一年度
   官方清单，系统会拒绝发布，不会把普通工作日猜成交易日。每年11月后，
   云端会自动尝试解析并保存上交所下一年度安排。
2. 自动解析 Federal Reserve 与美国 BEA 官方日程，按北京时间和下一个
   A股交易日映射冲击日期；抓取失败时读取上次成功缓存，页面会显示来源状态。
3. 每次预测写入 `data/performance_history.json`。目标交易日收盘后自动核对
   实际涨跌、近20次命中率、多数类基线和 Brier 分数；近期效果低于门限时，
   周方向自动降为“震荡”，逐日置信度自动降为“低”。

自动事件层只收录可核验官方日程，不把传闻、群聊截图或未确认新闻作为交易信号。

## 版本9：可信度优先

版本9不再用“每天必须给方向”作为目标：

1. 全市场宽度要求覆盖至少4000只A股。东方财富快照失败时切换新浪全A
   快照；双源同时失败或缓存超过3天时，周方向强制降为“震荡”，全部
   逐日置信度降为“低”。
2. 各模型先生成严格按时间切分的样本外概率，再用更早的样本外预测训练
   Sigmoid校准器、在更晚时间段评分，避免在同一批样本上校准和验证。
3. 实盘有效样本只采用“第1日预测的唯一目标交易日”，第2至第5日分别
   报告，不再把同一天的重叠期限当成多个独立样本。
4. 至少积累60个第1日目标日后才判断模型有效；要求相对多数类基线至少
   领先3个百分点且 Brier 低于0.25，才标记为近期有效。

## 版本10：行业可视化

版本10为行业方向增加可随每日数据自动更新的图形层：

1. 行业预期热力图汇总五日模型路径，颜色深浅只表示模型强弱，不代表
   确定涨跌。
2. 行业方向分布统计偏强、中性、偏弱数量，用于判断信号是否集中。
3. 每个行业提供近60个交易日与沪深300归一化对比曲线、相对强弱和
   近30日迷你走势图；图表采用收盘数据，不伪装成实时资金流。
4. 发布门禁要求每个行业至少包含50个交易日历史，并更新到大盘行情
   截止日；缺失或滞后时拒绝覆盖线上有效版本。

## 版本11：账号密码保护

Sites 当前动态运行层无法稳定执行服务端认证，因此版本11采用静态加密
保险箱，避免把失效的登录功能包装成安全功能：

1. 部署前使用 PBKDF2-SHA256（310,000次迭代）从密码派生256位密钥，
   再以 AES-256-GCM 加密完整预测 JSON。
2. Sites 构建删除全部明文预测副本，只发布带认证标签的密文；密码错误、
   密文被修改或密钥不匹配时都无法解密。
3. 登录成功后只把派生密钥保存在当前标签页的 `sessionStorage`，关闭
   标签页或点击“退出账号”即清除，不在仓库和网页文件中保存密码。
4. 构建时必须通过环境变量 `FORECAST_ACCESS_PASSWORD` 提供至少16位
   的高强度密码；构建结束后无需在 Sites 运行环境保存密码。

GitHub Pages 不支持服务端认证，因此它只能作为静态公开镜像，不能代替
受保护的 Sites 正式入口。若需要连源代码和历史预测数据也完全私有，
还需要将 GitHub 仓库改为私有，并调整自动更新的数据存储。

## 版本12：个人持仓日报

版本12在浏览器本地增加“我的持仓日报”，不连接券商，也不会把个人持仓
写入预测 JSON、GitHub 仓库或自动更新流水线：

1. 支持手动录入或 CSV 导入股票代码、名称、行业、成本价、现价和股数，
   并单独录入可用现金。现价由用户核对，页面不会把历史收盘价伪装成实时价。
2. 按规则检查总仓位、单票与行业集中度、高风险事件暴露，并把持仓行业
   映射到大盘五日路径、行业相对强弱和事件雷达。
3. 生成周一至周五条件式观察计划、逐项持仓诊断及与上次日报的变化；
   不输出保证收益、自动下单或无条件满仓指令。
4. 安全站点使用从登录密码独立派生的 AES-256-GCM 密钥加密本机持仓；
   密钥只保留在当前标签页。公开镜像使用本机设备密钥加密，安全级别较低。
5. 日报可导出为一个离线 HTML 文件，可直接打开或由浏览器打印为 PDF。

CSV 表头至少需要 `股票代码,成本价,现价,股数`，推荐完整表头：
`股票代码,股票名称,所属行业,成本价,现价,股数`。

## 版本15：可信度中控台与明日板块分层

版本15把“结果好看”与“证据可靠”拆成两个独立层级：

1. 大盘集成权重同时考虑全样本平衡准确率、最近240个样本的平衡准确率和
   Brier概率质量；近期模型获得更高权重，但保留完整历史锚点，降低追逐短期
   连胜造成的过拟合。
2. 历史相似状态只使用动量、趋势、ATR、宽度代理、风格与全球风险等核心
   状态特征，并加入轻微时间衰减；修正趋势效率量纲，使其稳定落在0–1附近。
3. 模型分歧会收缩最终概率；综合证据分同时显示近期基线优势、AUC、Brier、
   高置信命中率和模型一致性，证据不足时继续保留震荡输出。
4. 行业模型新增20/60日突破、ATR、趋势效率和量价确认特征；首页增加明日
   相对领先/落后分层，区分“正式偏强候选”和“仅相对排名观察”。
5. 前端新增可信度中控台、明日板块分层、移动端底部快捷导航和国内系统字体，
   减少外部字体依赖并改善手机访问与阅读层级。

## 版本16：次日行业排序的样本外校验

版本16专门收紧“明日领先/落后板块”的排序口径：

1. 行业超额收益回归改为按照近期样本外绝对误差动态加权，不再固定平均。
2. 每个行业公开超额收益回归相对零超额基线的误差、相关性与排序权重；表现偏弱的回归只保留小权重。
3. 新增按交易日分组的样本外“前三行业—后三行业”验证，报告顶底超额差、命中率与样本数；达标后才显示排序验证状态。
4. 页面明日板块卡片同步展示回归权重和排序验证，避免把未经验证的收益幅度当作确定结论。

## 测试

```powershell
py -m pytest
npm run verify:portfolio
```

如果没有安装 pytest：

```powershell
py -m pip install pytest
```

## 解释边界

样本外准确率略高于 50% 才是这类任务常见的真实尺度。预测概率会根据相对多数类基线的样本外能力向 50% 收缩；当模型分歧大或优势不足时，系统输出“震荡/观望”。第 2–5 日预测不应替代每日收盘后的重新训练。回测不是实盘承诺，政策、流动性、隔夜外盘和突发事件会使预测失效。
